# 🗺️ نقشه‌ی راه آینده‌ی Stkvpn (برداشت از سایر پروژه‌های فیلترشکن)

## مقدمه
در این سند، قابلیت‌هایی که در سایر اپ‌های مطرح فیلترشکن (مانند `GRoute`، `LxBox`، `WG Tunnel`، `dropweb`، `MahsaNG`، `NekoBox`، `v2rayNG` و ...) وجود دارند ولی در `Stkvpn` پیاده‌سازی نشده‌اند، گردآوری و دسته‌بندی شده‌اند. همچنین منبع هر قابلیت (پروژه‌ای که می‌توان از آن الهام گرفت) مشخص شده است. این سند به‌عنوان مرجع برای توسعه‌ی آینده‌ی پروژه استفاده خواهد شد.

---

## ۱. قابلیت‌های پیشرفته‌ی اتصال و هسته (Core & Connection)

| # | قابلیت | توضیح | منبع (پروژه) |
|---|--------|--------|---------------|
| ۱ | **Cloudflare WARP یک‌ضربه** | ثبت‌نام و اضافه‌کردن کانفیگ WARP (WireGuard یا MASQUE) با یک کلیک، بدون نیاز به کپی‌پیست دستی | WG Tunnel, GRoute, MahsaNG |
| ۲ | **MASQUE (WARP روی QUIC/HTTP-3)** | پروتکل جدید WARP که ترافیک را شبیه HTTPS معمولی می‌کند تا از تشخیص DPI فرار کند | Cloudflare WARP (اپ رسمی) |
| ۳ | **AmneziaWG و AWG 2.0** | نسخه‌ی مبهم‌سازی‌شده‌ی WireGuard برای مقاومت در برابر فیلترینگ | AmneziaVPN, lxbox (نسخه‌های جدید) |
| ۴ | **پشتیبانی از پروتکل‌های بیشتر** | NaïveProxy، SSH، SOCKS، Ping Tunnel | SagerNet, v2rayNG |
| ۵ | **حالت‌های چندگانه‌ی تونل** | کرنل (مجازی‌سازی مستقیم)، کاربر space، قفل‌شدن (kill switch سفارشی) و پروکسی (HTTP/SOCKS5) | v2rayNG, NekoBox |
| ۶ | **پورت‌ناک (Port Knocking) و ژنراتور پیلود** | برای دسترسی به گیت‌های مخفی یا دورزدن محدودیت‌ها | برخی کلاینت‌های اختصاصی |
| ۷ | **پشتیبانی همزمان از AmneziaWG و WireGuard** | امکان انتخاب بین دو پروتکل در یک اپ | AmneziaVPN |

---

## ۲. امنیت و حریم خصوصی (Security & Privacy)

| # | قابلیت | توضیح | منبع (پروژه) |
|---|--------|--------|---------------|
| ۱ | **ایزوله‌سازی پروکسی محلی** | بستن پورت پروکسی محلی (SOCKS/HTTP) برای جلوگیری از نشت IP توسط اپ‌های دیگر دستگاه | v2rayNG, NekoBox |
| ۲ | **مسیریابی هوشمند مبتنی بر ML** | انتخاب خودکار بهترین سرور با مدل LightGBM بدون پینگ‌های مداوم | GRoute |
| ۳ | **پروفایل‌های TLS مدرن** | شبیه‌سازی TLS مرورگرهای Firefox 148 و Safari 26 با تبادل کلید پسا-کوانتومی | Xray-core (پروژه‌ی اصلی) |
| ۴ | **تشخیص و مسدودسازی تبلیغات و ترکرها** | مشابه NetShield پروتون‌وی‌پی‌ان | Proton VPN, AdGuard VPN |
| ۵ | **احراز هویت بیومتریک** | قفل‌کردن اپ با اثر انگشت یا تشخیص چهره | WG Tunnel |
| ۶ | **گزارش خودکار کانفیگ‌های خراب** | ارسال بازخورد به سرور مرکزی برای پالایش کانفیگ‌ها | برخی کلاینت‌های اختصاصی |

---

## ۳. مدیریت شبکه و DNS (Network & DNS)

| # | قابلیت | توضیح | منبع (پروژه) |
|---|--------|--------|---------------|
| ۱ | **DNS-over-HTTPS (DoH) و DNS-over-TLS (DoT)** | رمزنگاری درخواست‌های DNS برای جلوگیری از رصد و فیلتر | lxbox, v2rayNG, NekoBox |
| ۲ | **DNS جداگانه (Split DNS)** | استفاده از DNS متفاوت برای دامنه‌های مختلف | Xray-core, sing-box |
| ۳ | **اسکنر IP پاک کلادفلر** | اسکن محدوده‌های IP کلادفلر و معرفی بهترین‌ها از نظر تأخیر | CloudflareScanner |
| ۴ | **تشخیص تغییرات شبکه و اتصال خودکار** | فعال‌سازی خودکار VPN هنگام اتصال به WiFi ناامن یا شبکه‌های خاص | v2rayNG, NekoBox |
| ۵ | **تشخیص DNS پویا** | به‌روزرسانی DNS بدون نیاز به ری‌استارت تونل | sing-box |

---

## ۴. مدیریت و کاربری (Management & UX)

| # | قابلیت | توضیح | منبع (پروژه) |
|---|--------|--------|---------------|
| ۱ | **اشتراک فایل (File Subscription)** | تبدیل یک فایل محلی چندنودی به اشتراک با قابلیت ویرایش منبع | NekoBox |
| ۲ | **واردات انبوه** | چسباندن چندین لینک کانفیگ (یکی در هر خط) و افزودن همه با یک کلیک | NekoBox, v2rayNG |
| ۳ | **انتخاب چندگانه و مدیریت دسته‌جمعی** | انتخاب چند سرور و کپی، اشتراک‌گذاری یا حذف یکجا | v2rayNG, NekoBox |
| ۴ | **تست همه‌ی سرورها (Test All)** | پینگ گرفتن از همه‌ی سرورها با یک کلیک و مرتب‌سازی بر اساس سریع‌ترین | v2rayNG, NekoBox |
| ۵ | **کاشی تنظیمات سریع (Quick Settings Tile)** | کنترل VPN از طریق منوی اعلان‌های اندروید بدون بازکردن اپ | v2rayNG |
| ۶ | **اتوماسیون مبتنی بر Intent** | کنترل تونل از طریق اپ‌های دیگر (مثل Tasker) | v2rayNG |
| ۷ | **پشتیبانی از Android TV** | اجرا روی تلویزیون‌های اندرویدی | lxbox, v2rayNG |
| ۸ | **تاریخچه‌ی دقیق مصرف داده** | نمایش مصرف به‌صورت ساعتی، روزانه و بازه‌ی دلخواه | v2rayNG, NekoBox |

---

## ۵. ابزارهای تست و عیب‌یابی (Diagnostic Tools)

| # | قابلیت | توضیح | منبع (پروژه) |
|---|--------|--------|---------------|
| ۱ | **تست کیفیت اینترنت** | اندازه‌گیری سرعت، پینگ، جیتر و تأخیر، همراه با امتیاز برای بازی، وبگردی، استریم و تماس ویدیویی | Speedtest by Ookla, NekoBox |
| ۲ | **تست تأخیر واقعی حین اتصال** | اندازه‌گیری پینگ واقعی از طریق تونل در حین اتصال | v2rayNG, NekoBox |
| ۳ | **پشتیبانی از لاگ Xray/Sing-box** | نمایش لاگ‌های موتور درون خود اپ | v2rayNG, NekoBox |
| ۴ | **حالت‌های مختلف TUN** | انتخاب بین TUN-mode، Proxy-only و VPN-mode | v2rayNG, sing-box |

---

## ۶. جمع‌بندی و اولویت‌بندی برای Stkvpn

بر اساس تأثیر بر تجربه‌ی کاربری و نیاز بازار، قابلیت‌های زیر برای پیاده‌سازی در اولویت هستند:

| اولویت | دسته‌بندی | قابلیت | منبع |
| :---: | :--- | :--- | :--- |
| ۱ | **هسته و اتصال** | Cloudflare WARP یک‌ضربه (WireGuard/MASQUE) | WG Tunnel, GRoute |
| ۲ | **امنیت** | DNS-over-HTTPS / DNS-over-TLS و Split DNS | lxbox, NekoBox |
| ۳ | **کاربری** | تست همه‌ی سرورها و انتخاب سریع‌ترین | v2rayNG, NekoBox |
| ۴ | **کاربری** | واردات انبوه و انتخاب چندگانه | NekoBox |
| ۵ | **ابزارها** | تست کیفیت اینترنت داخلی | NekoBox |
| ۶ | **امنیت** | احراز هویت بیومتریک | WG Tunnel |
| ۷ | **مدیریت** | کاشی تنظیمات سریع و اتوماسیون | v2rayNG |
| ۸ | **مدیریت** | پشتیبانی از Android TV | lxbox |

---

## ۷. نتیجه‌گیری

پروژه‌ی `Stkvpn` با معماری مدرن (Compose) و هسته‌ی قدرتمند (sing-box) پتانسیل بالایی برای تبدیل شدن به یکی از بهترین اپ‌های فیلترشکن اندروید را دارد. با افزودن تدریجی قابلیت‌های فوق (به‌ویژه موارد اولویت‌دار)، این اپ می‌تواند از رقبای خود پیشی بگیرد و تجربه‌ی کاربری بی‌نظیری ارائه دهد.

---
تاریخ: ۹ جولای ۲۰۲۶  
تهیه‌کننده: تیم توسعه‌ی Stkvpn
