# 📊 تحلیل پروژه‌های مرجع و برنامه‌ی عملیاتی Stkvpn

## ۱. مقدمه
هدف این سند، بررسی نقاط قوت سه پروژه‌ی مطرح در حوزه‌ی VPN بر پایه‌ی `sing-box` (یعنی `sfa`، `nekobox` و `lxbox`) و مقایسه‌ی آن‌ها با نقشه‌راه پروژه‌ی `Stkvpn` است. در نهایت، برنامه‌ای دقیق برای تکمیل فاز ۶ (بهینه‌سازی و انتشار) و تبدیل پروژه به یک هسته‌ی سبک و کارآمد ارائه می‌شود.

---

## ۲. معرفی و نقاط قوت سه پروژه

### 🔹 SFA (Sing-box for Android)
- **معماری**: دوماژول (`:core` و `:app`) – جداسازی کامل منطق VPN از UI.
- **هسته**: استفاده از `libbox.aar` (همان `sing-box`) به عنوان موتور اصلی.
- **نقاط قوت**:
  - ساختار تمیز و ماژولار که نگهداری و توسعه را آسان می‌کند.
  - پیاده‌سازی استاندارد `VpnService` با مدیریت صحیح Tun2Socks.
  - پشتیبانی از `PlatformInterface` برای تعامل با سیستم‌عامل.
  - استفاده از `Coroutine` و `Flow` برای مدیریت وضعیت اتصال.

### 🔹 NekoBox
- **معماری**: تک‌ماژول اما با پکیج‌بندی منظم (MVVM).
- **نقاط قوت**:
  - **UI فوق‌العاده زیبا و روان** با Jetpack Compose.
  - **مدیریت حرفه‌ای پروفایل‌ها** (ویرایش، import/export، کپی‌لینک).
  - **پشتیبانی از پروتکل‌های متنوع** (VLESS, VMess, Trojan, Shadowsocks, Hysteria2, WireGuard, TUIC).
  - **قابلیت‌های پیشرفته**: Domain Fronting، SNI Tunnel، Split Tunneling و Kill Switch.
  - **نمایش لحظه‌ای لاگ‌ها** با قابلیت فیلتر و جستجو.
  - **Backup/Restore** کامل با فرمت JSON.

### 🔹 LXBox
- **معماری**: مشابه SFA اما با تأکید بر **عبور از فیلترینگ**.
- **نقاط قوت**:
  - پیاده‌سازی **Fragment** و **Desync** برای دور زدن DPI.
  - قابلیت **Warp** (Cloudflare) به عنوان fallback.
  - بهینه‌سازی مصرف باتری و CPU با کاهش فرکانس آپدیت‌ها.
  - مدیریت خودکار **DNS-over-HTTPS** و **DNS-over-TLS**.

---

## ۳. مقایسه با نقشه‌راه Stkvpn

| فاز | عنوان | وضعیت در Stkvpn | منبع الهام (پروژه) |
|-----|-------|----------------|-------------------|
| ۱ | زیرساخت و پایه | ✅ کامل | SFA |
| ۲ | UI پایه | ✅ کامل (با Compose) | NekoBox |
| ۳ | مدیریت پروفایل‌ها | ✅ کامل | NekoBox |
| ۴ | پشتیبانی از پروتکل‌ها | ✅ کامل (باگ‌های جزئی) | SFA + NekoBox |
| ۵ | قابلیت‌های پیشرفته | ⏳ ناقص (Fronting, Split, KillSwitch) | NekoBox + LXBox |
| ۶ | بهینه‌سازی و انتشار | 🟡 در حال تکمیل | همه‌ی پروژه‌ها |

---

## ۴. وضعیت فعلی پروژه (بروزرسانی شده در ۹ جولای ۲۰۲۶)

### ✅ کارهای انجام‌شده

#### بخش‌های فاز ۶ (تکمیل‌شده)
1. **رفع مشکل اتصال (آیکون VPN)**  
   - پیاده‌سازی `BroadcastReceiver` برای دریافت وضعیت واقعی از سرویس  
   - حذف مقداردهی دستی `_isConnected` و جایگزینی با دریافت از سرویس  
   - **وضعیت**: ✅ کامل

2. **نمایش لحظه‌ای لاگ‌ها (Log Viewer)**  
   - ارسال لاگ‌ها از `V2RayService` از طریق `Broadcast`  
   - ایجاد `LogViewerScreen` با رنگ‌بندی سطوح (ERROR, WARN, SUCCESS, INFO)  
   - قابلیت پاک‌کردن لاگ‌ها و اسکرول خودکار به جدیدترین  
   - **وضعیت**: ✅ کامل

3. **نمایش مصرف داده‌ی واقعی (Download/Upload)**  
   - دریافت آمار ترافیک از `sing-box` (از طریق `getTrafficStats`)  
   - ارسال هر ۱ ثانیه از طریق `Broadcast`  
   - فرمت‌دهی خودکار (B, KB, MB, GB) در UI  
   - **وضعیت**: ✅ کامل

4. **تکمیل Backup/Restore (ذخیره‌سازی کامل تنظیمات)**  
   - ذخیره‌ی تمام وضعیت‌ها شامل: انتخاب‌شده، Fronting، SNI، Split Tunneling، لیست اپ‌ها  
   - استفاده از `FullBackupData` با قابلیت بازیابی کامل  
   - **وضعیت**: ✅ کامل

5. **مدیریت خطاها و نمایش Snackbar**  
   - اضافه‌شدن `_errorMessage` در ViewModel  
   - نمایش خطاها با `Snackbar` در `DashboardScreen`  
   - **وضعیت**: ✅ کامل

6. **بهبود کارایی LazyColumn**  
   - اضافه‌کردن `key = { it.id }` به لیست پروفایل‌ها برای جلوگیری از رندر مجدد کل لیست  
   - **وضعیت**: ✅ کامل

7. **بهینه‌سازی ProGuard/R8**  
   - اضافه‌شدن قوانین حفظ کلاس‌های `libbox` و `kotlinx.serialization`  
   - حذف لاگ‌ها در نسخه‌ی Release  
   - **وضعیت**: ✅ کامل

8. **مدیریت کلیک همزمان روی دکمه‌ی اتصال**  
   - استفاده از `Mutex` برای جلوگیری از Race Condition  
   - **وضعیت**: ✅ کامل

#### بخش‌های دیگر (تکمیل‌شده قبلی)
- [x] معماری دوماژول (الهام از SFA)
- [x] UI با Jetpack Compose (الهام از NekoBox)
- [x] پشتیبانی از پروتکل‌های اصلی (VLESS, VMess, Trojan, Shadowsocks, Hysteria2, WireGuard, TUIC)
- [x] مدیریت پروفایل‌ها (افزودن، ویرایش، حذف، انتخاب)
- [x] Smart Paste برای تشخیص خودکار لینک
- [x] تم شب/روز
- [x] Domain Fronting و SNI Tunnel (باگ‌دار – نیاز به بررسی بیشتر)

---

### ❌ کارهای باقی‌مانده (برای تکمیل فاز ۶)

#### با اولویت بالا (برای انتشار پایدار)
- [ ] **رفع باگ‌های Domain Fronting و SNI Tunnel**  
  - تست دقیق و اطمینان از عملکرد صحیح  
  - **وضعیت**: ⏳ در دست بررسی

- [ ] **نمایش زمان اتصال (مدت زمان)**  
  - محاسبه و نمایش زمان متصل بودن در UI  
  - **وضعیت**: ⏳ نیاز به پیاده‌سازی

- [ ] **رفع مشکل ذخیره‌سازی در اندروید ۱۱+**  
  - استفاده از `MediaStore` یا `SAF` برای دسترسی به فایل‌ها در نسخه‌های جدید  
  - **وضعیت**: ⏳ نیاز به بهبود

- [ ] **تست بر روی دستگاه‌های واقعی (اندروید ۶ تا ۱۴)**  
  - بررسی سازگاری و رفع باگ‌های احتمالی  
  - **وضعیت**: ⏳ در دست انجام

#### با اولویت متوسط (بهبود تجربه‌ی کاربری)
- [ ] **اضافه‌کردن قابلیت جستجو در لاگ‌ها**  
  - فیلتر بر اساس سطح یا کلمه‌ی کلیدی  
  - **وضعیت**: ⏳ برنامه‌ریزی‌شده

- [ ] **اضافه‌کردن نمایش سرعت لحظه‌ای**  
  - محاسبه و نمایش سرعت دانلود/آپلود بر اساس آمار ترافیک  
  - **وضعیت**: ⏳ برنامه‌ریزی‌شده

- [ ] **بهبود صفحه‌ی Settings**  
  - تکمیل تنظیمات Split Tunneling و Kill Switch  
  - **وضعیت**: ⏳ نیاز به توسعه

#### با اولویت کم (بهینه‌سازی‌های بیشتر)
- [ ] **کاهش مصرف باتری**  
  - کاهش فرکانس آپدیت‌ها در حالت عدم تغییر  
  - **وضعیت**: ⏳ بررسی‌شده

- [ ] **اضافه‌کردن قابلیت Warp (Cloudflare)**  
  - به عنوان fallback یا گزینه‌ی جداگانه  
  - **وضعیت**: 📅 برنامه‌ریزی‌شده برای فاز بعدی

---

## ۵. برنامه‌ی برداشت از هر پروژه

### از SFA:
- **معماری دوماژول** (در حال حاضر استفاده شده)
- **مدیریت `PlatformInterface`** و `TunOptions` – برای بهبود تعامل با هسته
- **روش ارسال وضعیت از سرویس** (با `BroadcastReceiver`)

### از NekoBox:
- **کامل‌ترین پیاده‌سازی Log Viewer** – شامل فیلتر، جستجو و ذخیره‌ی لاگ
- **مدیریت پیشرفته‌ی Backup/Restore** – شامل ذخیره‌ی تمام تنظیمات
- **UI زیبا و واکنش‌گرا** – برای بهبود تعامل کاربر

### از LXBox:
- **پیاده‌سازی Fragment و Desync** – برای دور زدن DPI (بخش فاز ۵)
- **مدیریت DNS پیشرفته** (DoH/DoT)
- **بهینه‌سازی مصرف باتری** – با کاهش آپدیت‌های غیرضروری

---

## ۶. اولویت‌بندی اقدامات باقی‌مانده

| اولویت | کار | منبع | زمان تخمینی |
|--------|-----|------|-------------|
| ۱ | رفع باگ‌های Fronting و SNI | LXBox | ۲ روز |
| ۲ | نمایش زمان اتصال | NekoBox | ۰.۵ روز |
| ۳ | بهبود ذخیره‌سازی (Android 11+) | SFA | ۱ روز |
| ۴ | تست روی دستگاه‌های واقعی | همه | ۲ روز |
| ۵ | جستجو در لاگ‌ها | NekoBox | ۱ روز |
| ۶ | نمایش سرعت لحظه‌ای | LXBox | ۱ روز |
| ۷ | تکمیل تنظیمات | NekoBox | ۲ روز |

---

## ۷. آمار کلی پروژه

| بخش | درصد پیشرفت | وضعیت |
|-----|-------------|--------|
| معماری و زیرساخت | ۱۰۰% | ✅ کامل |
| UI و تجربه‌ی کاربری | ۹۵% | 🟡 تقریباً کامل (نیاز به بهبودهای جزئی) |
| مدیریت پروفایل‌ها | ۱۰۰% | ✅ کامل |
| پشتیبانی از پروتکل‌ها | ۹۰% | 🟡 باگ‌های جزئی در Fronting |
| قابلیت‌های پیشرفته | ۶۰% | ⏳ نیاز به تکمیل (Split, KillSwitch, Warp) |
| بهینه‌سازی و انتشار | ۸۵% | 🟡 در حال تکمیل (تست و رفع باگ) |
| **مجموع** | **۸۸%** | **🟡 نزدیک به انتشار** |

---

## ۸. نتیجه‌گیری

پروژه‌ی `Stkvpn` با الهام‌گیری هوشمندانه از سه پروژه‌ی مطرح، به یک **هسته‌ی VPN قدرتمند و سبک** تبدیل شده است. با تکمیل موارد باقی‌مانده (به‌ویژه تست و رفع باگ‌های Fronting)، این پروژه آماده‌ی انتشار در فروشگاه‌های اندروید خواهد بود.

**گام بعدی**: تمرکز بر روی تست دستگاه‌های واقعی و رفع باگ‌های گزارش‌شده در Domain Fronting و SNI Tunnel.

---
تاریخ: ۹ جولای ۲۰۲۶  
تهیه‌کننده: تیم توسعه‌ی Stkvpn  
آخرین بروزرسانی: ۹ جولای ۲۰۲۶
