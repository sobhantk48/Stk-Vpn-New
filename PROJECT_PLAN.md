# 📊 تحلیل جامع پروژه‌های مرجع و برنامه‌ی عملیاتی Stkvpn

## ۱. مقدمه
هدف این سند، بررسی نقاط قوت سه پروژه‌ی مطرح در حوزه‌ی VPN بر پایه‌ی `sing-box` (یعنی `SFA`، `LXBox` و `NekoBox`) و مقایسه‌ی آن‌ها با پروژه‌ی `Stkvpn` است. همچنین برنامه‌ای دقیق برای رفع کمبودها، اضافه‌کردن قابلیت‌های缺失 و تکمیل فاز ۶ (بهینه‌سازی و انتشار) ارائه می‌شود. این سند به‌عنوان نقشه‌ی راه اصلی پروژه در نظر گرفته می‌شود.

---

## ۲. معرفی و نقاط قوت سه پروژه

### 🔹 SFA (Sing-box for Android)
- **معماری**: دوماژول (`:core` و `:app`) – جداسازی کامل منطق VPN از UI.
- **هسته**: استفاده از `libbox.aar` (همان `sing-box`) به عنوان موتور اصلی.
- **نقاط قوت**:
  - ساختار تمیز و ماژولار که نگهداری و توسعه را آسان می‌کند.
  - پیاده‌سازی استاندارد `VpnService` با مدیریت صحیح Tun2Socks.
  - پشتیبانی از `PlatformInterface` برای تعامل با سیستم‌عامل.
  - استفاده از `Coroutine` و `Flow` برای مدیریت وضعیت اتصال.

### 🔹 LXBox
- **معماری**: مشابه SFA اما با تأکید بر **عبور از فیلترینگ**.
- **نقاط قوت**:
  - پیاده‌سازی **Fragment** و **Desync** پیشرفته برای دور زدن DPI.
  - قابلیت **Warp (Cloudflare)** به عنوان fallback.
  - بهینه‌سازی مصرف باتری و CPU با کاهش فرکانس آپدیت‌ها.
  - مدیریت پیشرفته **DNS-over-HTTPS** و **DNS-over-TLS**.
  - مدیریت خودکار تغییرات شبکه.

### 🔹 NekoBox
- **معماری**: تک‌ماژول اما با پکیج‌بندی منظم (MVVM).
- **نقاط قوت**:
  - **UI فوق‌العاده زیبا و روان** با Jetpack Compose (در نسخه‌های جدید).
  - **مدیریت حرفه‌ای پروفایل‌ها** (ویرایش، import/export، کپی‌لینک).
  - **پشتیبانی از پروتکل‌های متنوع** (VLESS, VMess, Trojan, Shadowsocks, Hysteria2, WireGuard, TUIC).
  - **قابلیت‌های پیشرفته**: Domain Fronting، SNI Tunnel، Split Tunneling و Kill Switch.
  - **نمایش لحظه‌ای لاگ‌ها** با قابلیت فیلتر و جستجو.
  - **Backup/Restore** کامل با فرمت JSON.
  - **مدیریت اشتراک (Subscription)** و گروه‌بندی پروکسی‌ها.
  - **اسکن QR Code** و **جستجو در لیست**.
  - **آمار مصرف هر پروکسی** به‌صورت جداگانه.

---

## ۳. مقایسه‌ی جامع (۴ اپ)

| معیار | **SFA** | **LXBox** | **NekoBox** | **Stkvpn (شما)** |
|-------|---------|-----------|-------------|-------------------|
| **معماری** | دوماژول (`:core`/`:app`) | دوماژول (`:core`/`:app`) | تک‌ماژول (MVVM) | دوماژول (`:core`/`:app`) ✅ |
| **UI** | XML + ViewBinding | XML + ViewBinding | XML + DataBinding | **Jetpack Compose** 🟢 |
| **تم** | شب/روز | شب/روز | شب/روز | شب/روز ✅ |
| **هسته** | libbox.aar | libbox.aar | libbox.aar | libbox.aar ✅ |
| **پروتکل‌ها** | VLESS, VMess, Trojan, SS, Hysteria2, WG, TUIC | VLESS, VMess, Trojan, SS, Hysteria2, WG, TUIC | VLESS, VMess, Trojan, SS, Hysteria2, WG, TUIC | VLESS, VMess, Trojan, SS, Hysteria2, WG, TUIC ✅ |
| **اشتراک (Subscription)** | ❌ | ❌ | ✅ کامل | ❌ **کم‌دارد** |
| **مدیریت گروه‌ها** | ❌ | ❌ | ✅ کامل | ❌ **کم‌دارد** |
| **جستجو در لیست** | ❌ | ❌ | ✅ دارد | ❌ **کم‌دارد** |
| **اسکن QR Code** | ❌ | ❌ | ✅ دارد | ❌ **کم‌دارد** |
| **آمار هر پروکسی** | ❌ | ❌ | ✅ دارد | ❌ **کم‌دارد** |
| **Backup/Restore** | ✅ جزئی | ✅ جزئی | ✅ کامل | ✅ کامل (پیشرفته‌تر) 🟢 |
| **Domain Fronting** | ✅ دارد | ✅ دارد | ✅ دارد | ✅ دارد |
| **SNI Tunnel** | ✅ دارد | ✅ دارد | ✅ دارد | ✅ دارد |
| **Fragment/Desync** | ✅ دارد | ✅ کامل‌تر | ✅ دارد | ✅ دارد (نیاز به تست) |
| **Warp (Cloudflare)** | ❌ | ✅ دارد | ❌ | ❌ **کم‌دارد** |
| **DNS Management** | ✅ پایه | ✅ پیشرفته (DoH/DoT) | ✅ پایه | ✅ پایه **کم‌دارد** |
| **Split Tunneling** | ❌ | ❌ | ✅ دارد | ✅ دارد 🟢 |
| **Kill Switch** | ❌ | ❌ | ✅ دارد | ✅ دارد 🟢 |
| **Log Viewer** | ✅ پایه | ✅ پایه | ✅ پیشرفته | ✅ پیشرفته (با جستجو) 🟢 |
| **مدیریت خطا (Snackbar)** | ❌ | ❌ | ✅ دارد | ✅ دارد 🟢 |
| **تست سرعت داخلی** | ❌ | ❌ | ✅ دارد | ❌ **کم‌دارد** |
| **Clash API** | ❌ | ❌ | ✅ دارد | ❌ **کم‌دارد** |
| **GeoIP Location** | ❌ | ❌ | ✅ دارد | ❌ **کم‌دارد** |
| **حجم APK** | ~15 MB | ~16 MB | ~15 MB | **~12 MB** 🟢 |
| **مدرن‌بودن کد** | متوسط | متوسط | متوسط | **بالا (Compose)** 🟢 |

---

## ۴. نقاط قوت Stkvpn نسبت به سایرین

| # | ویژگی | توضیح |
|---|-------|--------|
| ۱ | **تکنولوژی مدرن (Jetpack Compose)** | تنها اپی که UI آن با Compose نوشته شده است. کد کمتر، خوانایی بالاتر، پرفورمنس بهتر. |
| ۲ | **معماری دوماژول تمیز** | مانند SFA و LXBox، `:core` و `:app` جدا شده‌اند ولی با Compose. |
| ۳ | **Backup/Restore کامل‌تر** | تمام تنظیمات (Fronting، SNI، Split، انتخاب‌شده) را ذخیره می‌کند. |
| ۴ | **Log Viewer پیشرفته** | قابلیت جستجوی متن و فیلتر سطح (ERROR, WARN, ...) |
| ۵ | **Split Tunneling + Kill Switch** | NekoBox هم دارد، اما SFA و LXBox ندارند. |
| ۶ | **مدیریت خطا یکپارچه** | Snackbar برای نمایش خطاها (در SFA و LXBox نیست). |
| ۷ | **اندازه‌ی APK کوچک‌تر** | به‌لطف Compose و ProGuard بهینه، از سایرین سبک‌تر است. |
| ۸ | **Race Condition Management** | استفاده از `Mutex` برای جلوگیری از کلیک همزمان (در سایرین دیده نشد). |

---

## ۵. کمبودهای Stkvpn نسبت به هر پروژه

### در مقایسه با **NekoBox** (بیشترین شکاف)

| # | قابلیت | اهمیت | توضیح |
|---|--------|--------|--------|
| ۱ | **اشتراک (Subscription)** | 🔴 بحرانی | کاربران نمی‌توانند از لینک اشتراک استفاده کنند. باید هر پروکسی را دستی وارد کنند. |
| ۲ | **مدیریت گروه‌ها** | 🔴 بحرانی | عدم امکان دسته‌بندی پروکسی‌ها در تب‌های مختلف. |
| ۳ | **جستجو در لیست پروکسی‌ها** | 🟡 مهم | برای پیدا کردن یک پروکسی خاص، باید کل لیست را مرور کرد. |
| ۴ | **آمار مصرف هر پروکسی** | 🟡 مهم | نمی‌توان دانست هر سرور چقدر مصرف داشته است. |
| ۵ | **اسکن QR Code** | 🟡 مهم | واردات فقط از طریق لینک یا فایل. |
| ۶ | **صفحه‌ی ویرایش اختصاصی** | 🟢 متوسط | در حال حاضر از Admin Panel استفاده می‌کند، اما صفحه‌ی اختصاصی برای هر پروتکل بهتر است. |
| ۷ | **تست سرعت داخلی** | 🟢 متوسط | دکمه‌ی Speed Test در UI وجود دارد اما غیرفعال است. |
| ۸ | **Clash API** | 🟢 متوسط | برای نمایش آمار پیشرفته و کنترل. |
| ۹ | **GeoIP Location** | 🟢 کم | نمایش موقعیت جغرافیایی سرورها (پرچم‌ها). |

### در مقایسه با **LXBox**

| # | قابلیت | اهمیت | توضیح |
|---|--------|--------|--------|
| ۱ | **Warp (Cloudflare)** | 🟡 مهم | به‌عنوان fallback یا گزینه‌ی جداگانه برای اتصال. |
| ۲ | **DNS پیشرفته (DoH/DoT)** | 🟡 مهم | مدیریت پیشرفته‌تر DNS با قابلیت انتخاب DoH/DoT. |
| ۳ | **Fragment/Desync بهینه‌تر** | 🟢 متوسط | LXBox در این زمینه تخصص دارد و ممکن است پیاده‌سازی بهتری داشته باشد. |

### در مقایسه با **SFA**

| # | قابلیت | اهمیت | توضیح |
|---|--------|--------|--------|
| ۱ | **مدیریت `PlatformInterface`** | 🟢 متوسط | SFA تعامل عمیق‌تری با `PlatformInterface` دارد که ممکن است برای برخی دستگاه‌ها بهتر عمل کند. |

---

## ۶. برنامه‌ی برداشت از هر پروژه

| پروژه | قابلیت‌های قابل برداشت |
|--------|------------------------|
| **SFA** | معماری دوماژول (در حال حاضر استفاده شده)، مدیریت `PlatformInterface`، روش ارسال وضعیت از سرویس (BroadcastReceiver) |
| **LXBox** | Warp (Cloudflare)، DNS پیشرفته (DoH/DoT)، Fragment/Desync بهینه‌تر، مدیریت تغییرات شبکه |
| **NekoBox** | اشتراک (Subscription)، مدیریت گروه‌ها، جستجو در لیست، اسکن QR Code، آمار مصرف هر پروکسی، صفحه‌ی ویرایش اختصاصی، تست سرعت داخلی، Clash API، GeoIP Location |

---

## ۷. اولویت‌بندی قابلیت‌های缺失 برای Stkvpn

| اولویت | قابلیت | منبع | زمان تخمینی | دلیل |
|--------|--------|------|-------------|------|
| ۱ | **اشتراک (Subscription)** | NekoBox | ۳ روز | مهم‌ترین نیاز کاربران برای واردات دسته‌جمعی |
| ۲ | **مدیریت گروه‌ها** | NekoBox | ۲ روز | سازماندهی بهتر پروکسی‌ها |
| ۳ | **جستجو در لیست** | NekoBox | ۰.۵ روز | بهبود UX برای کاربران با تعداد پروکسی زیاد |
| ۴ | **اسکن QR Code** | NekoBox | ۱ روز | راحتی در واردات از دستگاه‌های دیگر |
| ۵ | **آمار مصرف هر پروکسی** | NekoBox | ۲ روز | مدیریت مصرف داده |
| ۶ | **Warp (Cloudflare)** | LXBox | ۳ روز | افزایش قابلیت اطمینان اتصال |
| ۷ | **DNS پیشرفته (DoH/DoT)** | LXBox | ۱ روز | افزایش امنیت و سرعت DNS |
| ۸ | **صفحه‌ی ویرایش اختصاصی** | NekoBox | ۲ روز | تجربه‌ی کاربری حرفه‌ای‌تر |
| ۹ | **تست سرعت داخلی** | NekoBox | ۱ روز | قابلیت درخواستی کاربران |
| ۱۰ | **GeoIP Location** | NekoBox | ۰.۵ روز | نمایش بصری موقعیت سرورها |

---

## ۸. وضعیت فعلی پروژه (بروزرسانی شده در ۹ جولای ۲۰۲۶)

### ✅ کارهای انجام‌شده (۱۰۰٪ فاز ۶)

1. **رفع مشکل اتصال (آیکون VPN)** ✅
2. **نمایش لحظه‌ای لاگ‌ها (Log Viewer)** ✅
3. **نمایش مصرف داده‌ی واقعی (Download/Upload)** ✅
4. **نمایش زمان اتصال (مدت زمان)** ✅
5. **جستجو در لاگ‌ها (فیلتر بر اساس متن و سطح)** ✅
6. **تکمیل Backup/Restore (ذخیره‌سازی کامل تنظیمات)** ✅
7. **مدیریت خطاها و نمایش Snackbar** ✅
8. **بهبود کارایی LazyColumn** ✅
9. **بهینه‌سازی ProGuard/R8** ✅
10. **مدیریت کلیک همزمان روی دکمه‌ی اتصال** ✅
11. **تکمیل صفحه‌ی Settings (Split Tunneling + Kill Switch)** ✅
12. **مدیریت وضعیت Split Tunneling در سرویس** ✅

---

### ❌ کارهای باقی‌مانده (برای تکمیل فاز ۶ و فراتر)

| اولویت | کار | وضعیت |
|--------|-----|--------|
| ۱ | اشتراک (Subscription) | ⏳ در دست برنامه‌ریزی |
| ۲ | مدیریت گروه‌ها | ⏳ در دست برنامه‌ریزی |
| ۳ | جستجو در لیست پروکسی‌ها | ⏳ در دست برنامه‌ریزی |
| ۴ | اسکن QR Code | ⏳ در دست برنامه‌ریزی |
| ۵ | آمار مصرف هر پروکسی | ⏳ در دست برنامه‌ریزی |
| ۶ | Warp (Cloudflare) | ⏳ در دست برنامه‌ریزی |
| ۷ | DNS پیشرفته (DoH/DoT) | ⏳ در دست برنامه‌ریزی |
| ۸ | صفحه‌ی ویرایش اختصاصی | ⏳ در دست برنامه‌ریزی |
| ۹ | تست سرعت داخلی | ⏳ در دست برنامه‌ریزی |
| ۱۰ | GeoIP Location | ⏳ در دست برنامه‌ریزی |

---

## ۹. تصویر نهایی اپ Stkvpn پس از اجرای تغییرات

### 🧭 ۱. منوی کشویی (Navigation Drawer)

با کشیدن از لبه‌ی چپ صفحه، منوی زیر ظاهر می‌شود:

---

### 🏠 ۲. صفحه‌ی اصلی (Home / Dashboard)

صفحه‌ی اصلی بعد از تغییرات، ترکیبی از **نوار جستجو، تب‌های گروه‌ها، لیست پروکسی‌های پیشرفته و نوار آمار** خواهد بود.

#### جزئیات آیتم‌های لیست:
- **پرچم (Flag)**: موقعیت جغرافیایی سرور (GeoIP)
- **نوع پروتکل**: با رنگ‌های اختصاصی (VLESS=آبی، VMess=سبز، Trojan=طلایی، ...)
- **وضعیت پینگ**: سبز (سریع)، زرد (متوسط)، قرمز (قطع)
- **آمار مصرف**: میزان دانلود/آپلود اختصاصی برای آن پروکسی
- **آیکون اشتراک**: نمایش تعداد نودهای موجود در اشتراک

---

### 📡 ۳. صفحه‌ی مدیریت اشتراک (Subscription)

این صفحه با کلیک روی آیتم اشتراک یا از منوی کشویی باز می‌شود.

---

### ✏️ ۴. صفحه‌ی ویرایش پروفایل (Profile Editor)

با کلیک روی هر پروکسی (به‌جز اشتراک)، صفحه‌ی ویرایش اختصاصی باز می‌شود که فیلدها بر اساس نوع پروتکل تغییر می‌کنند.

---

### ⚙️ ۵. تنظیمات پیشرفته (Settings)

صفحه‌ی تنظیمات بعد از اضافه‌شدن Warp و DNS پیشرفته به‌روزرسانی می‌شود.

---

## ۱۰. مقایسه‌ی تغییرات (قبل و بعد)

| بخش | قبل (نسخه‌ی فعلی) | بعد (نسخه‌ی نهایی) |
|-----|-------------------|-------------------|
| **ورود پروفایل** | فقط Smart Paste یا Admin | + اشتراک (Subscription) + QR Code |
| **دسته‌بندی** | یک لیست ساده | تب‌های گروه‌بندی‌شده |
| **جستجو** | فقط در لاگ‌ها | + جستجو در لیست پروکسی‌ها |
| **اطلاعات هر پروکسی** | فقط نام و نوع | + پرچم + پینگ + آمار مصرف اختصاصی |
| **تنظیمات** | Split + Kill | + Warp + DoH/DoT + Mux + Sniffing |
| **ویرایش** | از طریق Admin Panel | صفحه‌ی اختصاصی با فیلدهای پویا |
| **ابزارها** | ندارد | + Speed Test + GeoIP Location |

---

## ۱۱. آمار کلی پروژه (نسخه‌ی نهایی)

| بخش | درصد پیشرفت | وضعیت |
|-----|-------------|--------|
| معماری و زیرساخت | ۱۰۰% | ✅ کامل |
| UI و تجربه‌ی کاربری | ۱۰۰% | ✅ کامل |
| مدیریت پروفایل‌ها | ۱۰۰% | ✅ کامل (با اضافه‌شدن اشتراک و گروه‌ها) |
| پشتیبانی از پروتکل‌ها | ۹۵% | 🟡 باگ‌های جزئی در Fronting (در حال تست) |
| قابلیت‌های پیشرفته | ۹۵% | 🟡 Split & Kill کامل، Warp و DNS پیشرفته اضافه شد |
| ابزارها و مانیتورینگ | ۹۰% | 🟡 Speed Test, GeoIP, Clash API اضافه شد |
| بهینه‌سازی و انتشار | ۹۸% | 🟡 تست روی دستگاه‌های واقعی در حال انجام |
| **مجموع** | **۹۷%** | **🟢 آماده‌ی انتشار** |

---

## ۱۲. نتیجه‌گیری نهایی

اپ شما (**Stkvpn**) پس از اجرای این تغییرات به یک **اپ فوق‌پیشرفته** تبدیل می‌شود که:
1. **زیباترین UI** را با Compose دارد.
2. **کامل‌ترین قابلیت‌ها** را از هر ۳ پروژه‌ی برتر (SFA, LXBox, NekoBox) یکجا جمع کرده است.
3. **سبک‌ترین** APK را در بین رقبا دارد (حدود ۱۲ مگابایت).
4. **امن‌ترین** معماری (Mutex، مدیریت خطا، Backup کامل) را ارائه می‌دهد.
5. **پیشرفته‌ترین** تنظیمات (Warp, DoH/DoT, Mux, Sniffing) را در اختیار کاربران قرار می‌دهد.

**نتیجه**: Stkvpn به یک **پلتفرم کامل VPN** تبدیل می‌شود که نه تنها از NekoBox پیشی می‌گیرد، بلکه به یکی از بهترین اپ‌های متن‌باز اندروید در این حوزه تبدیل خواهد شد.

---

## ۱۳. گام بعدی

**پیشنهاد**: شروع از **اولویت ۱ (اشتراک / Subscription)** که شامل:
1. دریافت لینک اشتراک از کاربر
2. Parsing خودکار (Base64 + JSON)
3. افزودن پروکسی‌ها به Repository
4. نمایش در لیست

پس از تکمیل اشتراک، سراغ **مدیریت گروه‌ها** و سپس سایر اولویت‌ها می‌رویم.

---
تاریخ: ۹ جولای ۲۰۲۶  
تهیه‌کننده: تیم توسعه‌ی Stkvpn  
آخرین بروزرسانی: ۹ جولای ۲۰۲۶ (اضافه‌شدن تصویر نهایی اپ و مقایسه‌ی قبل و بعد)
