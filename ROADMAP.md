# 📌 نقشه راه پروژه Stkvpn (نسخه نهایی)

## 🎯 هدف نهایی
تبدیل Stkvpn به یک اپلیکیشن VPN حرفه‌ای، پایدار و با قابلیت‌های پیشرفته با استفاده از بهترین‌های سه پروژه‌ی مرجع (SFA, NekoBox, LxBox).

---

## 📋 قوانین پروژه

| ردیف | عنوان قانون | توضیح |
|------|-------------|--------|
| ۱ | **قانون طلایی** | بررسی کامل ساختارها و در حافظه نگه داشتن فایل‌هایی که در این چت به من داده می‌شود. |
| ۲ | **قانون نقره‌ای** | بعد از هر تغییر، دستور کامیت و پوش روی برنچ main داده شود. |
| ۳ | **قانون برنزی** | همیشه به ریپو عمومی شما (`sobhantk48/Stkvpn`) نگاه کامل شود تا ببینم چه فایل‌هایی دارد. |
| ۴ | **قانون طلایی دوم** | من داخل Termux کد می‌زنم و داخل GitHub Actions بیلد می‌کنم. |
| ۵ | **قانون طلایی سوم** | بررسی کامل ساختارها و در حافظه نگه داشتن فایل‌هایی که به من داده می‌شود در این چت. |
| ۶ | **قانون دیباگ** | بعد از هر فاز، دیباگ انجام شود و تمام خطاها و لاگ‌ها به‌همراه راه‌حل‌هایشان در بخش دیباگ `ROADMAP.md` ثبت شوند. همچنین قبل از شروع فاز جدید، بیلد سبز تأیید شود. |
| ۷ | **قانون انتشار** | پس از اتمام فاز ۷ و تأیید بیلد سبز، نسخه‌ی اولیه در GitHub Releases منتشر شود. |
| ۸ | **قانون تست** | هر قابلیت جدید با حداقل یک کانفیگ واقعی تست شود. |
| ۹ | **قانون مستندسازی** | تمام تغییرات در `ROADMAP.md` و کامیت‌ها با پیام واضح ثبت شوند. |
| ۱۰ | **قانون امنیت** | اطلاعات حساس در GitHub Secrets ذخیره شوند. |
| ۱۱ | **قانون لاگ‌گیری** | تمام خطاهای runtime و استثناها در `Logger` ثبت شوند. |
| ۱۲ | **قانون بازخورد** | پس از هر تغییر، منتظر تأیید کاربر برای ادامه باشم. |
| ۱۳ | **قانون کدنویسی حرفه‌ای** | تمام کدها باید خط‌به‌خط بررسی شوند، بدون خطای کامپایل، با importهای کامل و ساختار صحیح، مانند یک دولوپر انسانی دقیق ارائه شوند. |

---

## 📅 فازبندی کلی

| فاز | عنوان | تعداد وظایف | وضعیت |
|-----|-------|-------------|--------|
| **فاز ۰** | آماده‌سازی و برنامه‌ریزی | ۲ | ✅ انجام شد |
| **فاز ۱** | معماری دوفرایندی (از SFA) | ۴ | ✅ انجام شد |
| **فاز ۲** | مدیریت اشتراک (از NekoBox) | ۳ | ✅ انجام شد |
| **فاز ۳** | Smart Paste و ConfigBuilder پیشرفته | ۳ | ⏳ در حال انجام |
| **فاز ۴** | پروتکل‌های جدید | ۴ | ❌ انجام نشده |
| **فاز ۵** | بهبود UI/UX | ۳ | ❌ انجام نشده |
| **فاز ۶** | بهینه‌سازی و انتشار | ۳ | ❌ انجام نشده |

---

## ✅ لیست دقیق وظایف (با وضعیت)

### فاز ۰: آماده‌سازی و برنامه‌ریزی
- [x] **۰-۱**: کلون کردن EasySNI و بررسی ساختار
- [x] **۰-۲**: تهیه نقشه راه و لیست وظایف

---

### فاز ۱: معماری دوفرایندی (از SFA)
- [x] **۱-۱**: ایجاد ماژول `bg` و `AndroidManifest.xml` برای فرایند جداگانه
- [x] **۱-۲**: انتقال `V2RayService` به فرایند `:bg` با `android:process=":bg"`
- [x] **۱-۳**: پیاده‌سازی `ServiceBinder` با AIDL و `RemoteCallbackList`
- [x] **۱-۴**: اصلاح `MainActivity` و `MainViewModel` برای استفاده از سرویس جدید

---

### فاز ۲: مدیریت اشتراک (از NekoBox)
- [x] **۲-۱**: ایجاد `SubscriptionEntity` و `SubscriptionDao` در Room
- [x] **۲-۲**: پیاده‌سازی `SubscriptionParser` برای دریافت و پارس اشتراک از URL
- [x] **۲-۳**: پیاده‌سازی `SubscriptionUpdater` با WorkManager و Gate‌های ضد اسپم

---

### فاز ۳: Smart Paste و ConfigBuilder پیشرفته
- [x] **۳-۱**: پیاده‌سازی `SmartParser` (تشخیص خودکار لینک‌ها، Clash YAML، v2rayN JSON)
- [x] **۳-۲**: اضافه کردن دیالوگ Smart Paste به `AdminScreen`
- [ ] **۳-۳**: ایجاد `ConfigBuilder` پیشرفته با پشتیبانی از VMess, Trojan, Shadowsocks, Hysteria, WireGuard

---

### فاز ۴: پروتکل‌های جدید (از LxBox + NekoBox)
- [ ] **۴-۱**: پشتیبانی از Hysteria2
- [ ] **۴-۲**: پشتیبانی از WireGuard
- [ ] **۴-۳**: پشتیبانی از TUIC v5
- [ ] **۴-۴**: پشتیبانی از AWG 2.0 (AmneziaWG) و XHTTP

---

### فاز ۵: بهبود UI/UX (از LxBox)
- [ ] **۵-۱**: نمایش پینگ لحظه‌ای (mass-ping)
- [ ] **۵-۲**: اضافه کردن تست سرعت داخلی
- [ ] **۵-۳**: نمایش ترافیک لحظه‌ای (دانلود/آپلود)

---

### فاز ۶: بهینه‌سازی و انتشار
- [ ] **۶-۱**: کاهش حجم APK با `shrinkResources` و `abiFilters`
- [ ] **۶-۲**: به‌روزرسانی ProGuard و حذف کدهای مرده
- [ ] **۶-۳**: انتشار نسخه‌ی اولیه در GitHub Releases

---

## 🛠️ بخش دیباگ (خطاهای رفع‌شده)

| ردیف | خطا | راه‌حل | وضعیت |
|------|-----|--------|--------|
| **D-01** | `Unresolved reference: V2rayAppTheme` | ایجاد `Theme.kt` با تابع `V2rayAppTheme` | ✅ |
| **D-02** | `Unresolved reference: SplitMode` | انتقال Enum به `model/SplitMode.kt` | ✅ |
| **D-03** | `config error: Listen on AnyIP but no Port(s)` | اصلاح ساختار JSON کانفیگ (آرایه‌ها) | ✅ |
| **D-04** | کرش بعد از `V2Ray started successfully` | اصلاح `port` به عدد و حذف `routing` | ✅ |
| **D-05** | `Unresolved reference: BuildConfig` | حذف `BuildConfig.DEBUG` و استفاده از منطق ساده | ✅ |
| **D-06** | AIDL `Couldn't find import for class IServiceCallback` | اضافه کردن `import` در `IService.aidl` | ✅ |
| **D-07** | `@Composable invocations` در `MainActivity` | استفاده از `by viewModels()` به‌جای `hiltViewModel()` | ✅ |
| **D-08** | `Could not find method implementation()` | اصلاح `build.gradle` (داخل بلوک `dependencies`) | ✅ |
| **D-09** | `SubscriptionUpdater` Type mismatch | اصلاح `collect` به `first()` و استفاده از `runBlocking` | ✅ |

---

## 📊 وضعیت فعلی

- **تاریخ شروع:** ۱۴۰۵-۰۴-۱۵
- **تاریخ به‌روزرسانی:** ۱۴۰۵-۰۴-۱۷
- **بیلد:** ✅ موفق (سبز)
- **اجرای اپ:** ✅ بدون کرش
- **VPN:** ✅ وصل می‌شود و علامت VPN بالا می‌آید
- **پنل ادمین:** ✅ کار می‌کند (رمز: `admin`)
- **مدیریت کانفیگ:** ✅ افزودن، ویرایش و حذف
- **مدیریت اشتراک:** ✅ فعال با WorkManager
- **Smart Paste:** ✅ فعال (تشخیص خودکار لینک‌ها و Clash YAML)
- **لاگ‌گیری:** ✅ فعال
- **پینگ:** ✅ کار می‌کند
- **دانلود/آپلود:** ❌ نیاز به توسعه (TrafficStats)

---

## 📌 گام‌های بعدی (اولویت‌بندی)

1. **🔴 تکمیل فاز ۳ (ConfigBuilder پیشرفته)** – فوری
2. **🟡 فاز ۴ (پروتکل‌های جدید)** – مهم
3. **🟢 فاز ۵ (بهبود UI/UX)** – تکمیلی
4. **🔵 فاز ۶ (بهینه‌سازی و انتشار)** – نهایی

---

## 📌 قوانین پروژه (به‌روز شده)

| ردیف | عنوان قانون | توضیح |
|------|-------------|--------|
| ۱ | **قانون طلایی** | بررسی کامل ساختارها و در حافظه نگه داشتن فایل‌هایی که در این چت به من داده می‌شود. |
| ۲ | **قانون نقره‌ای** | بعد از هر تغییر، دستور کامیت و پوش روی برنچ main داده شود. |
| ۳ | **قانون برنزی** | همیشه به ریپو عمومی شما (`sobhantk48/Stkvpn`) نگاه کامل شود تا ببینم چه فایل‌هایی دارد. |
| ۴ | **قانون طلایی دوم** | من داخل Termux کد می‌زنم و داخل GitHub Actions بیلد می‌کنم. |
| ۵ | **قانون طلایی سوم** | بررسی کامل ساختارها و در حافظه نگه داشتن فایل‌هایی که به من داده می‌شود در این چت. |
| ۶ | **قانون دیباگ** | بعد از هر فاز، دیباگ انجام شود و تمام خطاها و لاگ‌ها به‌همراه راه‌حل‌هایشان در بخش دیباگ `ROADMAP.md` ثبت شوند. همچنین قبل از شروع فاز جدید، بیلد سبز تأیید شود. |
| ۷ | **قانون انتشار** | پس از اتمام فاز ۶ و تأیید بیلد سبز، نسخه‌ی اولیه در GitHub Releases منتشر شود. |
| ۸ | **قانون تست** | هر قابلیت جدید با حداقل یک کانفیگ واقعی تست شود. |
| ۹ | **قانون مستندسازی** | تمام تغییرات در `ROADMAP.md` و کامیت‌ها با پیام واضح ثبت شوند. |
| ۱۰ | **قانون امنیت** | اطلاعات حساس در GitHub Secrets ذخیره شوند. |
| ۱۱ | **قانون لاگ‌گیری** | تمام خطاهای runtime و استثناها در `Logger` ثبت شوند. |
| ۱۲ | **قانون بازخورد** | پس از هر تغییر، منتظر تأیید کاربر برای ادامه باشم. |
| ۱۳ | **قانون کدنویسی حرفه‌ای** | تمام کدها باید خط‌به‌خط بررسی شوند، بدون خطای کامپایل، با importهای کامل و ساختار صحیح، مانند یک دولوپر انسانی دقیق ارائه شوند. |


## فاز ۴: پروتکل‌های جدید (Hysteria2, WireGuard, TUIC, AWG) ✅ کامل

### ۴-۱: Hysteria2
- **وضعیت:** ✅ کامل
- **تغییرات:**
  - اضافه شدن `hysteriaProtocolVersion`, `hysteriaAuthPayload`, `hysteriaObfs`, `hysteriaSni`, `hysteriaAllowInsecure`, `hysteriaUploadMbps`, `hysteriaDownloadMbps` به `Profile.kt`
  - اضافه شدن تابع `parseHysteria2` برای پارس لینک‌های `hysteria2://` و `hy2://`
  - به‌روزرسانی `SmartParser.kt` برای تشخیص لینک‌های Hysteria2
  - اضافه شدن `buildHysteria2Json` برای ساخت کانفیگ Sing-box
- **خطاها:** هیچ خطایی ثبت نشد

### ۴-۲: WireGuard
- **وضعیت:** ✅ کامل
- **تغییرات:**
  - اضافه شدن `wgLocalAddress`, `wgPrivateKey`, `wgPeerPublicKey`, `wgPreSharedKey`, `wgMtu`, `wgReserved` به `Profile.kt`
  - اضافه شدن `parseWireGuard` برای پارس لینک‌های `wireguard://`
  - به‌روزرسانی `SmartParser.kt` برای تشخیص لینک‌های WireGuard
  - اضافه شدن `buildWireGuardJson` برای ساخت کانفیگ Sing-box
  - تست موفق با لینک نمونه
- **خطاها:** هیچ خطایی ثبت نشد

### ۴-۳: TUIC v5
- **وضعیت:** ✅ کامل
- **تغییرات:**
  - اضافه شدن `tuicToken`, `tuicUuid`, `tuicCongestionController`, `tuicUdpRelayMode`, `tuicReduceRTT`, `tuicDisableSNI` به `Profile.kt`
  - اضافه شدن `parseTuic` برای پارس لینک‌های `tuic://`
  - به‌روزرسانی `SmartParser.kt` برای تشخیص لینک‌های TUIC
  - اضافه شدن `buildTuicJson` برای ساخت کانفیگ Sing-box
- **خطاها:** هیچ خطایی ثبت نشد

### ۴-۴: AWG (AmneziaWG)
- **وضعیت:** ✅ کامل
- **تغییرات:**
  - اضافه شدن پارامترهای AWG (`awgJc`, `awgJmin`, `awgJmax`, `awgS1`..`awgS4`, `awgH1`..`awgH4`, `awgI1`..`awgI5`) به `Profile.kt`
  - اضافه شدن `parseAwg` برای پارس لینک‌های `awg://`
  - اضافه شدن `parseAmneziaLink` برای پارس لینک‌های `vpn://` (Amnezia VPN)
  - اضافه شدن `parseAwgIni` برای پارس فایل‌های INI AWG
  - به‌روزرسانی `SmartParser.kt` برای تشخیص لینک‌های AWG
  - اضافه شدن `buildAwgJson` برای ساخت کانفیگ Sing-box با پارامترهای AWG
- **خطاها:** هیچ خطایی ثبت نشد

### جمع‌بندی فاز ۴:
- **وضعیت کلی:** ✅ کامل
- **پروتکل‌های پشتیبانی‌شده:** VLESS, VMess, Trojan, Shadowsocks, Hysteria2, WireGuard, TUIC, AWG
- **بیلد:** سبز ✅
- **تست‌های دستی:** WireGuard و AWG تست شدند و کار می‌کنند.

